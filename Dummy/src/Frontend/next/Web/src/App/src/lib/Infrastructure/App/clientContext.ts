import { api } from './Api/clientContext';
import { authentication } from './Authentication/clientContext';
import { localization } from './Localization/clientContext';

export const app = {
  api,
  authentication,
  localization,
};