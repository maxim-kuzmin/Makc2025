const login = {
  'lib.app.authentication.serverActions.login.ErrorMessage': 'Something went wrong.',
  'lib.app.authentication.serverActions.login.ErrorMessageForCredentialsSignin': 'Invalid credentials.',
} as const;

export default login;
